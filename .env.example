# Example environment for docker-compose/container setup only.
# Do not copy this file to the server application's local .env.
# For local server configuration, use server/.env.template instead.
# Any real .env file is gitignored and must not be committed.
NEO4J_USER=neo4j
NEO4J_PASSWORD=1234567890
