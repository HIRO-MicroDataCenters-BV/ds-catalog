apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "app.fullname" . }}-dashboard
  namespace: cattle-dashboards
  labels:
    grafana_dashboard: "1"
    {{- include "app.labels" . | nindent 4 }}
data:
  ds-catalog-dashboard.json: |-
{{ .Files.Get "dashboards/dashboard.json" | indent 4 }}
